#include "Base.xcconfig"

// Ambiente de PRODUCCIÓN (build Release).
APP_DISPLAY_NAME = Dinas Driver
PRODUCT_BUNDLE_IDENTIFIER = com.dinas.driver

// Producción real (HTTPS). El truco /$()/ evita que // se lea como comentario.
MIDDLEWARE_BASE_URL = https:/$()/wms-api.dinascorp.com/v1
